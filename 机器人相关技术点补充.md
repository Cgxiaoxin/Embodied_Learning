MoveIt2 的定位：不只是接口，是完整的运动规划框架
MoveIt2 做的事情分三层：
你的代码 (Python/C++)
    ↓ 调用
MoveIt2 框架
    ├── 运动学计算 (KDL / KDL_parser)       ← 正逆运动学
    ├── 路径规划器 (OMPL / CHOMP / STOMP)   ← 生成无碰撞轨迹
    ├── 碰撞检测 (FCL / Bullet)             ← Planning Scene
    └── 轨迹执行 (FollowJointTrajectory)    ← 下发给机器人
所以 MoveIt2 = 运动学 + 路径规划 + 碰撞检测 + 执行接口，是一个完整框架，不只是接口。

完整流程：从你写代码到机械臂动起来
第一层：URDF / SRDF 配置（前置必做）
robot.urdf     → 描述机械臂物理结构（连杆、关节、碰撞体）
robot.srdf     → 描述规划组、自碰撞禁止对、末端执行器
SRDF 是 MoveIt2 特有的，用 MoveIt Setup Assistant 生成：

定义 Planning Group（比如 arm 包含 joint1~joint7）
生成 Allowed Collision Matrix（哪些连杆对永远不用检测碰撞）

这一步做错，后面全错。

第二层：启动 MoveIt2 节点（运行时架构）
启动后运行的核心节点：

move_group_node          ← 核心，接收规划请求，调度规划器
robot_state_publisher    ← 发布 TF 树（各连杆坐标系）
rviz2 (可选)             ← 可视化 + 手动目标设置
move_group 是中枢，它同时：

订阅 /joint_states 获取当前关节角
维护 Planning Scene（障碍物地图）
接收规划请求，调用 OMPL 等规划器
将结果发送给执行器


第三层：你写代码调用 MoveIt2
方式A：Python MoveIt2 API（最常用）
pythonfrom moveit.planning import MoveItPy
from moveit.core.robot_state import RobotState

# 初始化
moveit = MoveItPy(node_name="my_planner")
arm = moveit.get_planning_component("arm")  # 对应 SRDF 里的 planning group

# 方式1：给末端执行器的目标位姿（笛卡尔空间）
from geometry_msgs.msg import PoseStamped
target_pose = PoseStamped()
target_pose.header.frame_id = "base_link"
target_pose.pose.position.x = 0.5
target_pose.pose.position.z = 0.3
target_pose.pose.orientation.w = 1.0
arm.set_goal_state(pose_stamped_msg=target_pose, pose_link="end_effector")

# 方式2：给关节角目标（关节空间）
arm.set_goal_state(configuration_name="home")  # SRDF 里定义的命名状态

# 规划
plan_result = arm.plan()
if plan_result:
    moveit.execute(plan_result.trajectory, controllers=[])
方式B：C++ API（性能敏感场景）
cpp#include <moveit/move_group_interface/move_group_interface.h>
#include <moveit/planning_scene_interface/planning_scene_interface.h>

// 规划组
auto move_group = MoveGroupInterface(node, "arm");

// 设置目标位姿
geometry_msgs::msg::Pose target;
target.position.x = 0.5;
target.orientation.w = 1.0;
move_group.setPoseTarget(target);

// 规划 + 执行
moveit::planning_interface::MoveGroupInterface::Plan plan;
if (move_group.plan(plan) == MoveItErrorCode::SUCCESS) {
    move_group.execute(plan);
}

第四层：规划器内部发生了什么（OMPL）
你调 arm.plan()
    ↓
MoveIt2 调用 OMPL（Open Motion Planning Library）
    ├── 当前状态 → 随机采样 → 构建搜索树（RRT* / PRM）
    ├── 每个采样点：调用 FCL 碰撞检测 vs Planning Scene
    ├── 找到无碰撞路径 → 路径平滑（shortcutting）
    └── 输出 JointTrajectory（带时间戳的关节角序列）
OMPL 里常用的规划算法：

RRTConnect：默认，双向搜索，速度快
RRT*：渐近最优，路径更短
CHOMP：梯度优化，路径更平滑，适合码垛这类重复任务


第五层：轨迹执行（MoveIt2 → 实际机器人/仿真）
MoveIt2 生成 JointTrajectory
    ↓
trajectory_execution_manager
    ↓
发送 FollowJointTrajectory action 到 Controller
    ↓
          ┌─────────────────────────────────┐
          │  真实机器人：硬件 controller    │
          │  Isaac Sim：ros2_control 插件   │
          └─────────────────────────────────┘
Isaac Sim 这边需要启用 ros2_control 插件，它实现了 FollowJointTrajectory action server，接收 MoveIt2 的轨迹指令后驱动仿真关节。

Isaac ROS：NVIDIA 的 ROS2 加速包
Isaac ROS 不是替代 MoveIt2 的，它是加速 ROS2 感知层，给 MoveIt2 提供更好的"眼睛"。
Isaac ROS 做的事：

相机/激光雷达 → Isaac ROS 感知包 → 输出给 MoveIt2 Planning Scene
                    │
            ┌───────┴────────┐
            │                │
      nvblox (3D重建)    isaac_ros_object_detection
      实时障碍物地图      检测纸箱位置和姿态
            │                │
            └───────┬────────┘
                    ↓
          MoveIt2 Planning Scene (障碍物更新)
                    ↓
              规划无碰撞轨迹
Isaac ROS 核心组件：
nvblox — 实时 3D 场景重建，输出 Voxel 地图给 MoveIt2 作为动态障碍物，这对码垛场景（纸箱位置不固定）非常关键。
isaac_ros_cumotion — 这是最重要的：把 MoveIt2 的规划器替换成 cuRobo（GPU加速），规划速度从毫秒级提升到微秒级。
标准 MoveIt2：      OMPL (CPU) → 规划一条路径 ~200-500ms
isaac_ros_cumotion：cuRobo (GPU) → 规划一条路径 ~5-20ms
完整的 Isaac ROS 码垛流程：
深度相机
    ↓ (点云)
isaac_ros_nvblox
    ↓ (voxel map)         isaac_ros_object_detection (检测纸箱)
    ↓                              ↓ (纸箱 6D 位姿)
MoveIt2 Planning Scene ←──────────┘
    ↓ (障碍物地图 + 目标位姿)
isaac_ros_cumotion (cuRobo 规划器)
    ↓ (无碰撞轨迹)
ros2_control (Isaac Sim) 或 真实机器人 controller
    ↓
机械臂执行抓取

对你的项目的具体建议
针对烟草码垛 + 15-30kg 重载机械臂：
规划器选择：先用 MoveIt2 + RRTConnect 跑通基本流程，demo 阶段够用。后期如果需要快速重规划（比如纸箱位置有偏差需要实时调整），再引入 cuMotion。
障碍物管理：码垛场景障碍物（货架、已码垛纸箱）位置相对固定，可以直接在 MoveIt2 的 Planning Scene 里手动添加碰撞箱（CollisionObject），不一定需要 nvblox。
关节空间 vs 笛卡尔空间：

取货/放货用笛卡尔空间规划（指定末端位姿）
回原点用关节空间规划（指定关节角，速度快且可预测）
抓取接近段用笛卡尔直线路径（computeCartesianPath，保证末端沿直线插入）